╔══════════════════════════════════════════════════════════════════╗
║              ADMIN MODULE - QUICK REFERENCE CARD                 ║
╚══════════════════════════════════════════════════════════════════╝

📍 ADMIN DASHBOARD URL
   http://localhost:5001/admin.html

🔑 DEFAULT CREDENTIALS
   Email:    admin@careerguidance.com
   Password: Admin@123

⚠️  IMPORTANT: Change password after first login!

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🚀 SETUP COMMANDS

1. Database Setup
   .\setup-admin-db.ps1

2. Build Application
   dotnet build

3. Run Application
   dotnet run

4. Test Endpoints (optional)
   .\test-admin-endpoints.ps1

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📡 API ENDPOINTS

Authorization: Bearer <token>

GET  /api/admin/users
     ?page=1&pageSize=20&search=john
     → List all users with pagination

GET  /api/admin/users/{id}
     → Get detailed user information

DELETE /api/admin/users/{id}
       → Delete user and all related data

PUT  /api/admin/users/{id}/role
     Body: {"role": "admin"}
     → Update user role

GET  /api/admin/stats
     → Get system statistics

GET  /api/admin/export/users
     → Export users as CSV

GET  /api/admin/analytics/growth?days=30
     → Get user growth analytics

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🔧 COMMON TASKS

Create New Admin User:
  UPDATE Users SET Role = 'admin' WHERE Email = 'user@example.com';

Verify Admin Role:
  SELECT Id, Username, Email, Role FROM Users WHERE Role = 'admin';

Check Database Schema:
  DESCRIBE Users;
  SHOW TABLES LIKE 'admin%';

Reset Admin Password:
  1. Generate hash: BCrypt.Net.BCrypt.HashPassword("NewPass123")
  2. UPDATE Users SET PasswordHash = '<hash>' WHERE Email = 'admin@careerguidance.com';

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📊 DASHBOARD FEATURES

✓ System Statistics
  - Total users
  - Active users (today/week)
  - Videos watched
  - Resumes created
  - Average progress

✓ User Management
  - Paginated list
  - Search functionality
  - Sort by multiple columns
  - View detailed profiles
  - Delete users
  - Promote to admin

✓ Analytics
  - Popular career paths
  - Recent activities
  - User growth trends

✓ Export
  - Download CSV
  - All user data

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🐛 TROUBLESHOOTING

401 Unauthorized
→ Check token is valid and user has admin role

Can't login
→ Verify admin user exists: SELECT * FROM Users WHERE Email = 'admin@careerguidance.com';
→ Verify Role column exists: DESCRIBE Users;

Page not loading
→ Check backend is running: dotnet run
→ Verify URL is correct: http://localhost:5001/admin.html

No data showing
→ Check users exist: SELECT COUNT(*) FROM Users;

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📚 DOCUMENTATION

ADMIN_SETUP_COMPLETE.md     → Complete setup guide
ADMIN_MODULE_GUIDE.md       → Detailed API documentation
ADMIN_QUICK_SETUP.md        → Quick setup instructions
ADMIN_PROCESSING_SUMMARY.md → Processing summary

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ STATUS: READY TO USE

All components verified and tested.
Build successful. No critical errors.
Database migration ready.
Setup scripts created.
Testing scripts available.

🎉 Your admin module is production-ready!

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
